# DECISIONS.md: התלבטויות והחלטות

קובץ חי. כל החלטה כתובה כך: מה הוחלט, למה, ומה החלופה שנדחתה. החלטות פתוחות מסומנות **פתוח**.

---

## 1. זרימת העבודה

**D1. שמונה שלבים במערכת, אחד-לאחד מול ששת השלבים שהוגדרו.**
- השלבים שהוגדרו: עסק, חובה, 10 קונספטים, קומפוזיציה, ביקורת, פרומפט סופי.
- במערכת שלב 6 ("פרומפט סופי") מתפצל לשלושה: 6 יצירת אלמנטים, 7 הרכבה ואיחוד, 8 טקסט וייצוא.
- הסיבה: הדרישה החדשה היא שכל אלמנט ייווצר ויאושר בנפרד. לכן "הפרומפט הסופי" הוא בפועל סט פרומפטים: אחד לכל אלמנט, ואחד לאיחוד (`fusionPrompt`).
- בבקשה נכתב "חמשת השלבים". הנחתי שהכוונה לתהליך שהגדרנו (6), ולא השמטתי אף שלב.

**D2. פירוק לאלמנטים נעשה כבר בשלב הקומפוזיציה (4).**
- המודל מחזיר `elements[]`: בדיוק רקע אחד (`background`), ו-1 עד 5 אובייקטים (`object`), לכל אחד `layout` כשבר מהפריים ו-`z`.
- השרת מנרמל (`normalizePlan`): מוודא רקע יחיד, מפתחות ייחודיים, clamp לערכים, ומקסימום 8 אלמנטים.
- החלופה שנדחתה: לפרק רק אחרי הביקורת. נדחתה כי הביקורת צריכה לבקר גם את הפירוק עצמו.

**D3. הביקורת מחזירה `revised_plan`, והמשתמש בוחר בין המשופרת למקורית.**
- "נעילת" התוכנית (`lock-plan`) יוצרת או מסנכרנת את טבלת `elements`.
- אלמנט שהפרומפט או הסוג שלו השתנו מאבד את האישור. שאר האלמנטים שומרים עליו.

**D4. אובייקטים נוצרים על רקע כרומה אחיד (ברירת מחדל `#00FF00`, או מג'נטה או כחול), והסרת הרקע נעשית בדפדפן.**
- הסיבה: מודלי תמונה לא מחזירים alpha אמיתי בצורה אמינה.
- כרומה עם tolerance, feather ו-despill ב-Canvas (`keyOut`) פשוטה, מהירה ושקופה למשתמש, כולל סליידר סף.
- החלופה שנדחתה: מודל segmentation בצד שרת. זו תלות כבדה, וזה לא נדרש ב-v1.
- **פתוח**: מוצר שבעצמו ירוק או מג'נטה ידרוש לבחור צבע כרומה אחר. זה קיים בממשק, אבל אין זיהוי אוטומטי.

**D5. "חיבור" האלמנטים נעשה בשני שלבים: קולאז' מיקום, ואחריו איחוד (fusion) ב-Gemini.**
- המשתמש ממקם בגרירה.
- הקולאז' נשלח כ-Image 1, יחד עם כל התמונות המאושרות כרפרנסים, ובקשה לרנדר מחדש כצילום אחד עם תאורה וצללים משותפים.
- הסיבה: הדבקה פשוטה של שכבות נראית "מודבקת", כי אין צללי מגע ואין צבעוניות משותפת.
- יש גם כפתור "שימוש בקולאז' כמו שהוא", למי שמרוצה או רוצה שליטה מלאה.
- **סיכון ידוע**: האיחוד עלול לשנות פרטים באלמנט מאושר, למשל פרטי אריזה. לכן:
  - הפרומפט דורש נאמנות לרפרנסים.
  - אפשר לבחור את הקולאז' כבסיס במקום האיחוד.
  - תמונה של מוצר אמיתי מומלץ להעלות ("העלאת צילום") במקום לייצר.

**D6. טקסט, לוגו ופונטים לעולם לא עוברים דרך מודל התמונה.**
- הם מוצבים ב-Canvas מעל תמונת הבסיס (`renderAd`).
- הסיבה: מודלי תמונה משבשים עברית ולא מכירים פונטים של המשתמש.
- כך גם מובטח שהטקסט יוצא בדיוק כפי שנכתב.
- אותה פונקציה מרנדרת את התצוגה ואת הייצוא, ולכן מה שמאשרים הוא מה שיוצא.

**D7. הטקסטים נבנים בשרת משדות החובה, ולא מהמודל (`buildTextLayers`).**
- המודל מחליט רק על מיקום לפי תפקיד (`text_layout`). את התוכן הוא לא כותב.
- זו אכיפה של "אל תשנה או תמציא פרטים": שדה ריק לא מופיע.
- בשלב 8 הטקסט לא ניתן לעריכה. לשינוי נוסח עורכים את שלב 2 ולוחצים "רענון טקסטים", והעיצוב נשמר.

## 2. פונטים

**D8. ~~פונט שייך למשתמש, ואפשר לשתף אותו לארגון.~~ הוחלף ב-D32.**
- `fonts.owner_user_id` הוא חובה, ו-`fonts.org_id` אופציונלי.
- פונט שבו `org_id` הוא NULL הוא אישי: זמין רק לבעליו, בכל הארגונים שלו.
- פונט עם `org_id` זמין לכל חברי אותו ארגון.
- מחיקה: הבעלים של הפונט, או owner/admin של הארגון.
- כך מתקיימות שתי הדרישות: "כל משתמש מעלה פונטים שלו", וגם צוות שעובד על מותג משותף.

**D9. טעינה בדפדפן דרך `FontFace` API עם `fetch` מאומת, ולא דרך `@font-face` ב-CSS.**
- הקובץ מוגש רק למי שמורשה (cookie session).
- לכל פונט נקבע שם משפחה פנימי `uf<ID>`, ולכן שם שהמשתמש בוחר לא יכול להתנגש או להזריק CSS.
- יש fallback ל-Arial או sans-serif אם הטעינה נכשלת.

**D10. אימות קובץ פונט לפי magic bytes (TTF, OTF, WOFF, WOFF2), עם מגבלה של 10MB.**
- הסיומת וה-MIME שהלקוח שולח לא נחשבים.
- **פתוח**:
  - אין עדיין פענוח של טבלת `name` כדי לחלץ שם משפחה ומשקל אוטומטית. המשתמש מקליד אותם.
  - אין בדיקת רישיון. יש הערה בממשק.

## 3. נתונים, ארגונים והרשאות

**D11. SQLite (`better-sqlite3`) עם migrations ממוספרות (`PRAGMA user_version`).**
- הסיבה: אפס תשתית, הרצה במכונה אחת, טרנזקציות סינכרוניות פשוטות.
- החלופה שנדחתה: SQL Server, הסטנדרט אצלנו. נדחתה ל-v1 בגלל עלות ההקמה.
- השכבה מבודדת ב-`db.js`, וה-SQL סטנדרטי כמעט לגמרי: `datetime('now')` ו-AUTOINCREMENT יצטרכו התאמה.
- **פתוח**: אם עוברים ל-multi-instance או ל-SQL Server, זה הדבר הראשון להחליף.

**D12. יחס רבים-לרבים: `memberships(user_id, org_id, role)`.**
- התפקידים הם `owner`, `admin` ו-`member`.
- כל נרשם מקבל ארגון אישי שבו הוא owner.
- ארגון חייב להישאר תמיד עם owner אחד לפחות (409).
- רק owner ממנה owner, או מסיר או משנה owner.

**D13. ~~הוספת חבר לפי אימייל של משתמש שכבר רשום. אין הזמנות במייל.~~ הוחלף ב-D30.**
- הסיבה: אין תשתית מייל ב-v1.
- **פתוח**: טבלת `invitations` עם טוקן, אם יידרש.

**D14. גישה למשאבים דרך חברות בארגון בכל שאילתה.**
- משאב של ארגון שהמשתמש לא חבר בו מחזיר 404 ולא 403, כדי לא לחשוף שהוא קיים.
- זה חל על project, element, asset וקובץ פונט.
- כל member יכול לערוך את כל פרויקטי הארגון. אין הרשאות ברמת פרויקט.

**D15. assets ב-DB עם קבצים בדיסק (`DATA_DIR/files/p<projectId>/<uuid>.<ext>`).**
- `abs()` חוסם path traversal.
- מחיקת פרויקט מוחקת את הקבצים שלו.
- **גיבוי**: צריך לגבות את כל `DATA_DIR`, כלומר את ה-DB ואת הקבצים יחד.

## 4. Gemini

**D16. כל קריאה ל-Gemini רצה על המפתח של המשתמש המבצע. אין מפתח שרת.**
- המפתח מוצפן ב-AES-256-GCM, עם מפתח שנגזר מ-`APP_SECRET`.
- הוא אף פעם לא מוחזר ל-client, רק `••••1234`.
- השרת מאמת אותו מול `GET /v1beta/models` לפני שמירה.
- משמעות: חבר ארגון בלי מפתח לא יכול ליצור (שגיאה `no_gemini_key`), אבל יכול לצפות ולהרכיב.
- **פתוח**: מפתח ברמת ארגון, אם יידרש חיוב מרוכז.
- **סיכון**: שינוי `APP_SECRET` הופך את כל המפתחות השמורים לבלתי ניתנים לפענוח. המשתמשים יצטרכו להזין מחדש, והממשק מציג את זה.

**D17. שני מודלים שניתן להגדיר.**
- `GEMINI_TEXT_MODEL=gemini-2.5-flash` לשלבי הטקסט, עם `responseMimeType: application/json`.
- `GEMINI_IMAGE_MODEL=gemini-2.5-flash-image` לתמונות, עם `responseModalities: ["IMAGE"]` ו-`imageConfig.aspectRatio`.
- **לא אומת מול ה-API האמיתי**: סביבת הבנייה חסומה ל-`generativelanguage.googleapis.com`. שמות המודלים ומבנה הבקשה נכתבו לפי התיעוד המוכר לי, ויש לאמת אותם בהרצה ראשונה.

**D18. `GEMINI_MOCK=1` הוא מוק דטרמיניסטי ואופליין.**
- הוא מחזיר JSON מובנה לכל שלב, ו-PNG אמיתי (encoder מינימלי ב-`png.js`) בגודל וביחס הנכונים.
- אובייקטים מקבלים רקע כרומה אמיתי, כדי שגם הסרת הרקע תיבדק.
- הבדיקות וההדגמות רצות בלי מפתח ובלי עלות.

**D19. פרומפטים לתמונה באנגלית, תוכן למשתמש בעברית.**
- `style` (lighting, palette, lens, mood) נכתב באנגלית ומוזרק לכל פרומפט של אלמנט ושל איחוד, כדי לשמור על עקביות תאורה בין אלמנטים שנוצרים בנפרד.
- כל פרומפט של תמונה אוסר במפורש טקסט, לוגו ו-watermark.

**D20. "תיקון הגרסה הנבחרת" שולח את התמונה הקודמת כ-input לעריכה, יחד עם ההערה.**
- "גרסה חדשה" עם הערה מייצרת מאפס, וההערה מתווספת לפרומפט.
- יש שני מצבים כי לפעמים רוצים שינוי קטן ולפעמים כיוון אחר.

## 5. אבטחה

**D21. Session עם cookie HttpOnly ו-SameSite=Lax (Secure ב-production). נשמר רק SHA-256 של הטוקן.**
- הגנת CSRF: כל בקשה שמשנה נתונים חייבת לכלול `X-Requested-With: adstudio`, כותרת שטופס cross-site לא יכול לשלוח.
- הסיבה ל-cookie ולא ל-Bearer: תגיות `<img>` ו-`fetch` לפונטים עובדות ישירות, בלי טוקן ב-URL.

**D22. סיסמאות ב-`scrypt` (מובנה ב-Node), עם salt לכל משתמש.**

**D23. תמונות שעולות (אלמנט, קולאז', לוגו, ייצוא) מאומתות לפי magic bytes: PNG, JPEG או WEBP.**
- SVG לא מתקבל, כי הוא וקטור XSS.

**פתוח (לפני חשיפה לאינטרנט):**
- rate limiting ל-login ול-generate
- שחזור סיסמה
- אימות אימייל
- מכסות יצירה לארגון
- audit log

## 6. Frontend

**D24. Vue 3 + vue-router + Vite, בלי ספריית UI ובלי Pinia.**
- ה-state הגלובלי קטן: `me` ו-`orgId`, ולכן `reactive` פשוט מספיק.
- ה-state של הפרויקט מסופק לשלבים ב-`provide/inject`.

**D25. Canvas ברזולוציה מלאה, מוקטן ב-CSS, עם overlay של divs לגרירה (`Stage.vue`).**
- החלופה שנדחתה: Konva או Fabric. הן תלות כבדה, והצורך כאן הוא רק move, resize ומקלדת.
- ה-resize שומר יחס לתמונות. לטקסט הגובה נגזר מהתוכן.

**D26. עיצוב בשפת proofing של דפוס.**
- משטח אפור ניטרלי, כדי לשפוט צבעים בתמונות בלי הטיה.
- סימני חיתוך סביב הקנבס.
- חותמת "מאושר" היא האלמנט הבולט היחיד.
- Magenta לפעולה ראשית.
- פונטים: Assistant ו-Suez One (תומכים בעברית).

## 7. בדיקות

**D27. שתי שכבות בדיקה, שתיהן עוברות.**
- `server/test/api.test.js` (node:test): בדיקת API מלאה מול שרת אמיתי ו-DB זמני. היא מכסה:
  - הרשמה, התחברות ו-CSRF
  - ריבוי ארגונים והרשאות, ו-owner אחרון
  - הצפנה ומיסוך של המפתח
  - נראות פונטים אישי מול ארגוני
  - כל שלבי הצינור
  - יצירה, תיקון, העלאה ואישור של אלמנטים
  - איחוד וייצוא
  - איפוס אישור כשפרומפט משתנה
  - חסימת MIME מזויף
  - מחיקה עם קבצים
- `e2e/run_e2e.py` (Playwright + Chromium): הדפדפן עובר את כל הזרימה דרך הממשק, כולל:
  - העלאת פונט TTF אמיתי ובדיקה שהוא נטען ב-`document.fonts`
  - גרירה בקנבס
  - ייצוא PNG ובדיקה שהוא בדיוק 1080×1350
  - משתמש שני שמצטרף לארגון, רואה את הפרויקט, ולא רואה את הפונט האישי של הראשון
  - אפס שגיאות קונסול

## 8. ידוע ולא נעשה (backlog)

- פענוח טבלת `name` בפונט
- הזמנות במייל
- rate limiting
- מכסות
- undo/redo בהרכבה
- snap וקווי עזר
- גרסאות של plate מול טקסט
- ייצוא בכמה פורמטים בבת אחת (אותו קמפיין ב-1:1 וב-9:16)
- תבניות מותג לארגון (צבעים, פונטים ולוגו ברירת מחדל)
- מעבר ל-SQL Server
- שינוי פורמט אחרי יצירת רקע דורש ליצור את הרקע מחדש. אין אזהרה אוטומטית מעבר להערה בשלב 1.

## 9. התקנה

**D28. השרת מותקן עם `npm ci --ignore-scripts`.**
- `better-sqlite3` v13 מגיע עם קבצים בינאריים מוכנים בתוך החבילה (`prebuilds/`, כולל `win32-x64` ו-`linux-x64`).
- בלי הדגל, npm מריץ `node-gyp rebuild` כי יש `binding.gyp`. זה דורש כלי בנייה של C++ (ב-Windows זה Visual Studio Build Tools) וגישה ל-nodejs.org.
- ל-express ול-multer אין סקריפטים הכרחיים.
- נבדק: התקנה נקייה מה-zip, ואחריה כל הבדיקות עוברות.


---

# גרסה 2: הכנה לחשיפה לאינטרנט וספריות משותפות

## 10. הגבלת קצב (rate limiting)

**D29. limiter מסוג sliding window בזיכרון (`ratelimit.js`), וכל הערכים מרוכזים ב-`LIMITS`.**

| פעולה | מפתח | מגבלה |
|---|---|---|
| התחברות | אימייל + IP | 10 ב-15 דק' |
| התחברות | אימייל | 100 ב-15 דק' |
| התחברות | IP | 50 ב-15 דק' |
| הרשמה | IP | 20 בשעה |
| שכחתי סיסמה | IP | 10 בשעה |
| שכחתי סיסמה | אימייל | 3 בשעה (שקט) |
| איפוס סיסמה | IP | 20 בשעה |
| צפייה או קבלה של הזמנה | IP | 30 בשעה |
| שליחת הזמנות | ארגון | 50 ביום |
| אימות מפתח Gemini | משתמש | 10 בשעה |
| יצירה | משתמש | 30 בדקה |
| יצירה | ארגון | 120 בדקה |

- **למה הפיצול בהתחברות**: מגבלה לפי אימייל בלבד מאפשרת לתוקף לנעול משתמש לגיטימי (DoS). הפיצול חוסם brute force ממקור אחד, ומשאיר את הנעילה של החשבון עצמו גבוהה.
- **"שכחתי סיסמה" לפי אימייל מוגבל בשקט**: התשובה לא משתנה, ולכן אי אפשר לגלות אם הכתובת רשומה.
- **כותרת `X-Forwarded-For` לא נחשבת כברירת מחדל (`TRUST_PROXY` כבוי).**
  - בלי זה, לקוח יכול לזייף IP ולעקוף את כל המגבלות לפי IP. יש על זה בדיקה.
  - מאחורי IIS, nginx או Cloudflare Tunnel צריך להגדיר `TRUST_PROXY=1`.
- **תגובה**: 429 עם `Retry-After` והודעה בעברית.
- **מגבלה**: המצב נשמר בזיכרון של תהליך אחד.
  - באתחול הוא מתאפס.
  - בכמה instances כל אחד סופר לבד.
  - **פתוח**: מעבר ל-Redis, או ל-Cloudflare Rate Limiting כשכבה חיצונית, כשיהיה יותר מ-instance אחד.

## 11. מייל, הזמנות ושחזור סיסמה

**D30. הזמנות במייל מחליפות את ההוספה הישירה.**
- מנהל שולח הזמנה עם תפקיד. הטוקן אקראי ורק ה-hash שלו נשמר, והוא תקף ל-7 ימים ולשימוש אחד.
- ההזמנה **קשורה לכתובת**: רק משתמש שמחובר עם אותו אימייל יכול לקבל אותה, ולכן קישור שהועבר לאחר לא עובד.
- המוזמן רואה תצוגה מקדימה בלי להתחבר, עם קישורי התחברות או הרשמה שהאימייל כבר ממולא בהם.
- הזמנה חוזרת לאותה כתובת מבטלת את הקודמת. מנהל יכול לבטל, ורואה רשימת הזמנות ממתינות.
- **הסיבה**: הסכמה. אף אחד לא נכנס לארגון בלי לאשר, וגם אפשר להזמין מי שעוד לא רשום.

**D31. שחזור סיסמה.**
- `POST /auth/forgot` מחזיר תמיד את אותה תשובה.
- הטוקן תקף לשעה ולשימוש אחד. יש טוקן חי אחד למשתמש, ובקשה חדשה מבטלת את הקודם.
- האיפוס **מנתק את כל ה-sessions** של המשתמש, ואז מחבר אותו מחדש במכשיר הנוכחי.
- כל הקישורים במייל נבנים מ-`APP_URL` ולא מכותרת ה-Host של הבקשה. כך נמנע host-header injection, שבו תוקף גורם לקישור איפוס להפנות לדומיין שלו.
- **מנגנון שליחה (`mailer.js`)**:
  - `smtp` ב-production דרך nodemailer 10. בחרתי בגרסה האחרונה כי ב-7.x יש פגיעויות (npm audit).
  - `outbox` בפיתוח ובבדיקות: קובצי JSON ב-`DATA_DIR/outbox`, שמהם הבדיקות קוראות את הקישורים.
- **Production לא עולה בלי `SMTP_URL` ובלי `APP_URL`**, אלא אם מגדירים במפורש `MAIL_TRANSPORT=outbox`.
- שליחת מייל האיפוס היא fire-and-forget, כדי שכשל או זמן תגובה לא יחשפו אם החשבון קיים.
- שליחת הזמנה כן מחזירה שגיאה, כי המנהל צריך לדעת שהיא לא יצאה.
- **פתוח**: אין אימות אימייל בהרשמה.
  - אפשר להירשם עם כתובת של מישהו אחר.
  - זה לא מאפשר לקבל הזמנה שלו, כי צריך את הטוקן שנשלח לתיבה שלו.
  - הבעלים האמיתי יכול להשתלט על החשבון דרך איפוס סיסמה, שגם מנתק את כל ה-sessions.

## 12. מכסות

**D32a. מכסה חודשית לארגון, נפרדת לטקסט ולתמונות.**
- ברירת מחדל מ-`QUOTA_TEXT_MONTHLY=300` ו-`QUOTA_IMAGE_MONTHLY=200`, והחודש לפי UTC.
- כל קריאה ל-Gemini עוברת דרך `metered()`, שבודקת בסדר הזה:
  1. burst limit למשתמש ולארגון.
  2. המכסה החודשית. אם היא נוצלה: 429 עם `quota_exceeded` ותאריך ההתחדשות.
  3. הקריאה עצמה.
  4. רישום ב-`usage_events`, **רק אם הקריאה הצליחה**.
- **חריגה אפשרית**: קריאות מקבילות יכולות לעבור את המכסה במספר הקריאות שכבר בדרך. זה עדיף על חיוב של קריאות שנכשלו.
- **מי קובע מכסה**: רק מנהל מערכת, לפי `PLATFORM_ADMINS` (רשימת אימיילים). בעלים של ארגון לא יכול להגדיל לעצמו. ערך `null` מחזיר לברירת המחדל.
- כל חברי הארגון רואים את השימוש. מנהל מערכת גם עורך את המכסה מאותו מסך, ויש לו `GET /admin/orgs`.
- **הערה**: המפתח עדיין של המשתמש, ולכן המכסה מגינה על המערכת (עומס, שימוש לרעה) ולא על חשבון החיוב.

## 13. ספריות משותפות: פונטים ואלמנטים

**D32. הבעלות על פונטים ואלמנטים היא של הארגון, ולכל פריט יש נראות `org` או `public`.**
- `org`: כל חברי הארגון רואים ומשתמשים.
- `public`: כל משתמש מחובר בכל ארגון יכול להשתמש, לשימוש בלבד.
- אין הרשאות לארגון מסוים, כפי שהתבקש: "זמין לכולם", ולא ניהול הרשאה לכל אחד בנפרד.
- **ניהול** (שם, נראות, מחיקה): מי שהוסיף את הפריט, או בעלים ומנהלים של הארגון שהפריט שלו.
- **ספרייה לפי ארגון**: הרשימה בארגון X מראה את הפריטים של X, ואת הפריטים הציבוריים של ארגונים אחרים. היא לא מערבבת פריטים לא-ציבוריים מארגונים אחרים שהמשתמש חבר בהם.
  - **הסיבה**: לא להדליף בטעות נכסים של לקוח א' למודעה של לקוח ב', רק כי אותו אדם חבר בשניהם.
  - מעבר בין ארגונים נעשה רק דרך `public`.
- **הגנה על מידע אישי**: שם היוצר מוצג רק בתוך הארגון שלו. ארגונים אחרים רואים רק את שם הארגון.
- **אזהרה לפני פרסום**: הממשק שואל לפני שמסמנים "כולם", ומזכיר לבדוק את רישיון הפונט.

**D33. מיגרציה v2 של פונטים.** פונט "אישי" מ-v1 עובר לארגון הוותיק ביותר שהמשתמש הוא בעלים שלו. אם אין כזה, הוא עובר לארגון כלשהו שהמשתמש חבר בו. הנראות שלו נקבעת כ-`org`.
- **שינוי התנהגות**: פונט שהיה פרטי הופך לגלוי לחברי אותו ארגון. ברוב המקרים זה הארגון האישי ("הסטודיו של X"), שבו לרוב אין אנשים נוספים.
- פונט של משתמש בלי אף חברות בארגון נמחק מהטבלה, והקובץ נשאר על הדיסק.

**D34. "כל אלמנט נשמר": כל אלמנט **שמאושר** נשמר אוטומטית בספריית הארגון.**
- לכל פריט בספרייה יש **עותק קובץ משלו** (`files/library/o<org>/…`), ולכן הוא נשמר גם אם הפרויקט נמחק.
- השמירה אידמפוטנטית לפי `source_asset_id`: אישור חוזר לא יוצר כפילות.
- גרסה שנלקחה מהספרייה (`assets.from_library_id`) לא נשמרת שוב.
- **למה רק גרסאות מאושרות אוטומטית**: שמירת כל גרסה שנוצרה (כולל ניסיונות כושלים) הייתה ממלאת את הספרייה ברעש.
- גרסה שלא אושרה אפשר לשמור ידנית בכפתור "שמירת הגרסה לספרייה".
- **שימוש בשלב 6**: הכפתור "מהספרייה" פותח בחירה מסוננת לפי סוג (רקע או אובייקט) ומקור, עם חיפוש. הפריט מועתק כגרסה חדשה של האלמנט, וצבע הכרומה שלו עובר איתו.
- **פתוח**: תגיות, תיקיות, ומחיקה אוטומטית של פריטים ישנים.

## 14. בדיקות (עודכן)

- **API: 20 בדיקות**. חדש בגרסה הזו:
  - הזמנות: חשבון שגוי, שימוש יחיד, החלפה, ביטול.
  - ספריית פונטים: org מול public, הרשאות ניהול, scope.
  - ספריית אלמנטים: שמירה אוטומטית, מניעת כפילות, שמירה ידנית, שימוש חוצה-ארגונים, שרידות אחרי מחיקת פרויקט.
  - מכסות: חסימה, אי-ספירה של כשלים, מנהל מערכת, חזרה לברירת מחדל.
  - איפוס סיסמה: תשובה זהה, ניתוק sessions, שימוש יחיד, מגבלה לפי אימייל.
  - rate limit להתחברות, כולל ניסיון עקיפה ב-XFF מזויף.
  - burst ביצירה.
- **E2E**: בנוסף לזרימה המלאה, נבדקים גם:
  - העלאת פונט לספרייה.
  - בחירה מהספרייה בשלב האלמנטים.
  - תצוגת הספרייה עם האלמנטים שנשמרו.
  - מסך השימוש.
  - הזמנה במייל למשתמש חדש: הרשמה מהקישור, קבלה, והפרויקט המשותף.
  - שחזור סיסמה מקצה לקצה.
